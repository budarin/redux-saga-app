import windowSize from './clientWindowSize';
import deviceInfo from './clientDeviceInfo';
import currentRouter from './currentRouter';

const clientReducers = {
    currentRouter,
    windowSize,
    deviceInfo,
};

export default clientReducers;
